apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
data:
  database: tes_dev
  my.cnf: |
    [mysqld]
    max_connections=50
    character-set-server=utf8mb4
    collation-server=utf8mb4_unicode_ci